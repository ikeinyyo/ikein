from .run import run

methods = {
    "run": {
        "method": run,
        "info": "Manage and run to predefined command aliases.",
        "usage": "ikein run [-a <alias> <command>] | [<alias>] | [-l] | [-r <alias>]",
    }
}
